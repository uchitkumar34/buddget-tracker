// let arr = [{ a: 1, b: 2 }, { c: 3, d: 4 }, { a: 1, b: 2 }, { c: 3, d: 4 }];

// const newArr = arr.filter((item, index) =>
//     index === arr.findIndex(obj => Object.keys(obj).every(key => obj[key] == item[key]) 
//     )
// );

// console.log(newArr);

